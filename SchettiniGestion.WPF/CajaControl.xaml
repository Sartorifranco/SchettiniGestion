<UserControl x:Class="SchettiniGestion.WPF.CajaControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SchettiniGestion.WPF"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200"
             Background="{DynamicResource ModuleBackgroundBrush}"
             Loaded="CajaControl_Loaded">

    <Grid Margin="20">
        <Grid.Resources>
            <Style x:Key="CajaModalCancelButtonStyle" TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderDarkBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Padding" Value="16,10"/>
                <Setter Property="MinWidth" Value="100"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource HoverBackground}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Control de Caja y Movimientos" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource HeaderForegroundBrush}" Margin="0,0,0,20"/>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{DynamicResource PanelBackgroundBrush}" CornerRadius="10" Padding="20" Margin="0,0,20,0" BorderBrush="{DynamicResource BorderDarkBrush}" BorderThickness="1">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="💰" FontSize="40" VerticalAlignment="Center" Margin="0,0,20,0"/>
                    <StackPanel>
                        <TextBlock Text="SALDO EN CAJA" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextSecondary}"/>
                        <TextBlock x:Name="lblSaldo" Text="$ 0,00" FontSize="40" FontWeight="Bold" Foreground="{StaticResource SuccessColor}"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <StackPanel Grid.Column="1" Orientation="Horizontal">

                <Button x:Name="btnIngreso" Click="btnIngreso_Click" Margin="0,0,10,0" Cursor="Hand"
                        Style="{StaticResource SuccessButtonStyle}" MinWidth="150" Padding="20,10">
                    <StackPanel>
                        <TextBlock Text="⬆ INGRESAR" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <TextBlock Text="Dinero / Ajuste" Foreground="White" Opacity="0.88" FontSize="12" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button x:Name="btnEgreso" Click="btnEgreso_Click" Cursor="Hand"
                        Style="{StaticResource DeleteButtonStyle}" MinWidth="150" Padding="20,10">
                    <StackPanel>
                        <TextBlock Text="⬇ RETIRAR" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <TextBlock Text="Gasto / Retiro" Foreground="White" Opacity="0.88" FontSize="12" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

            </StackPanel>
        </Grid>

        <Border Grid.Row="2" Background="{DynamicResource PanelBackgroundBrush}" CornerRadius="5" Padding="10" BorderBrush="{DynamicResource BorderDarkBrush}" BorderThickness="1">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="Movimientos del Día" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,10"/>

                <DataGrid x:Name="dgvMovimientos" Style="{StaticResource ModuleDataGridStyle}" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding CajaID}" Width="60"/>
                        <DataGridTextColumn Header="Hora" Binding="{Binding Fecha, StringFormat={}{0:HH:mm}}" Width="80"/>
                        <DataGridTextColumn Header="Concepto" Binding="{Binding Concepto}" Width="*"/>
                        <DataGridTextColumn Header="Tipo" Binding="{Binding Tipo}" Width="100"/>
                        <DataGridTextColumn Header="Monto" Binding="{Binding Monto, StringFormat=C2}" Width="120" FontWeight="Bold"/>
                        <DataGridTextColumn Header="Usuario" Binding="{Binding Usuario}" Width="120"/>
                    </DataGrid.Columns>
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
                        </Style>
                    </DataGrid.RowStyle>
                </DataGrid>
            </DockPanel>
        </Border>

        <!-- Overlay a pantalla completa del contenido del control (último en Z-order para cubrir todo) -->
        <Grid x:Name="popupMovimiento"
              Grid.Row="0" Grid.Column="0"
              Grid.RowSpan="10" Grid.ColumnSpan="10"
              Visibility="Collapsed"
              Background="#80000000"
              Panel.ZIndex="1000">
            <Border Width="400"
                    MinHeight="260"
                    Background="White"
                    CornerRadius="12"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Padding="24"
                    BorderBrush="{DynamicResource BorderDarkBrush}"
                    BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="2" BlurRadius="10" Opacity="0.3" Direction="270" Color="#000000"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock x:Name="lblPopupTitulo" Text="Registrar Movimiento"
                               FontSize="20" FontWeight="Bold"
                               Foreground="{DynamicResource TextPrimary}"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,18"/>

                    <StackPanel Grid.Row="1" VerticalAlignment="Center">
                        <TextBlock Text="Monto:" Style="{StaticResource ModuleLabelStyle}"/>
                        <xctk:DecimalUpDown x:Name="numMontoManual" Style="{StaticResource ModuleNumericUpDownStyle}"
                                            FormatString="C2" Margin="0,0,0,20" FontSize="18"/>

                        <TextBlock Text="Concepto / Motivo:" Style="{StaticResource ModuleLabelStyle}"/>
                        <TextBox x:Name="txtConceptoManual" Style="{StaticResource ModuleTextBoxStyle}"
                                 Height="80" TextWrapping="Wrap" AcceptsReturn="True"/>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                        <Button Content="Cancelar"
                                Style="{StaticResource CajaModalCancelButtonStyle}"
                                Margin="0,0,10,0"
                                Click="btnCancelarPopup_Click"/>
                        <Button Content="Guardar"
                                Style="{StaticResource ButtonStyle}"
                                MinWidth="110"
                                Padding="18,10"
                                Click="btnGuardarMovimiento_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

    </Grid>
</UserControl>