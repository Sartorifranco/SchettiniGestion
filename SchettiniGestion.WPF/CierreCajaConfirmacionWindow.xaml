<Window x:Class="SchettiniGestion.WPF.CierreCajaConfirmacionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirmar cierre"
        Width="560"
        SizeToContent="Height"
        MinHeight="320"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent">

    <Border Background="{DynamicResource PanelBackgroundBrush}"
            BorderBrush="{DynamicResource BorderColor}"
            BorderThickness="1"
            CornerRadius="12"
            Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="Confirmar cierre"
                               FontSize="22"
                               FontWeight="Bold"
                               Foreground="{DynamicResource TextPrimary}"/>
                    <TextBlock Text="Revisá los totales del turno antes de registrar el cierre. Al confirmar se cierra el turno: para seguir vendiendo hay que abrir caja de nuevo. Las ventas ya registradas no se borran."
                               FontSize="13"
                               Foreground="{DynamicResource TextSecondary}"
                               Margin="0,6,0,0"
                               TextWrapping="Wrap"/>
                </StackPanel>
                <Button Content="✕"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Width="44"
                        Height="44"
                        Style="{StaticResource SecondaryButtonStyle}"
                        FontSize="16"
                        Click="btnCancelar_Click"/>
            </Grid>

            <Border Grid.Row="1"
                    Background="{DynamicResource SurfaceDark}"
                    CornerRadius="8"
                    Padding="18,14"
                    Margin="0,0,0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="SALDO APERTURA"
                                   Foreground="{DynamicResource TextSecondary}"
                                   FontSize="11"/>
                        <TextBlock x:Name="lblSaldoApertura"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource TextPrimary}"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                        <TextBlock Text="INGRESOS"
                                   Foreground="{DynamicResource TextSecondary}"
                                   FontSize="11"/>
                        <TextBlock x:Name="lblIngresos"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource SuccessColor}"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                        <TextBlock Text="EGRESOS"
                                   Foreground="{DynamicResource TextSecondary}"
                                   FontSize="11"/>
                        <TextBlock x:Name="lblEgresos"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource DangerColor}"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="3" HorizontalAlignment="Right">
                        <TextBlock Text="SALDO CIERRE"
                                   Foreground="{DynamicResource TextSecondary}"
                                   FontSize="11"/>
                        <TextBlock x:Name="lblSaldoCierre"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource SuccessColor}"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <TextBlock Grid.Row="2"
                       Text="¿Confirma el cierre de caja?"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimary}"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>

            <StackPanel Grid.Row="3"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="Cancelar"
                        Style="{StaticResource SecondaryButtonStyle}"
                        MinWidth="130"
                        MinHeight="48"
                        FontSize="15"
                        Margin="0,0,10,0"
                        Click="btnCancelar_Click"/>
                <Button Content="Confirmar cierre"
                        Style="{StaticResource SuccessButtonStyle}"
                        MinWidth="170"
                        MinHeight="48"
                        FontSize="15"
                        FontWeight="SemiBold"
                        Click="btnConfirmar_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
