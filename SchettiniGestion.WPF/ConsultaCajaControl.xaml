<UserControl x:Class="SchettiniGestion.WPF.ConsultaCajaControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BackgroundDark}"
             Loaded="Control_Loaded">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Consulta de Caja" FontSize="22" FontWeight="Bold"
                   Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,15"/>

        <Border Grid.Row="1" Background="{DynamicResource SurfaceDark}" CornerRadius="8" Padding="20,16" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="SALDO ACTUAL" Foreground="{DynamicResource TextSecondary}" FontSize="12"/>
                    <TextBlock x:Name="lblSaldoActual" FontSize="28" FontWeight="Bold" Foreground="{StaticResource SuccessColor}"/>
                </StackPanel>
                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                    <TextBlock Text="INGRESOS HOY" Foreground="{DynamicResource TextSecondary}" FontSize="12"/>
                    <TextBlock x:Name="lblIngresosHoy" FontSize="22" FontWeight="Bold" Foreground="{StaticResource SuccessColor}"/>
                </StackPanel>
                <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                    <TextBlock Text="EGRESOS HOY" Foreground="{DynamicResource TextSecondary}" FontSize="12"/>
                    <TextBlock x:Name="lblEgresosHoy" FontSize="22" FontWeight="Bold" Foreground="{StaticResource DangerColor}"/>
                </StackPanel>
                <StackPanel Grid.Column="3" HorizontalAlignment="Right">
                    <TextBlock Text="ÚLTIMA ACTUALIZACIÓN" Foreground="{DynamicResource TextSecondary}" FontSize="12"/>
                    <TextBlock x:Name="lblUltimaAct" FontSize="14" Foreground="{DynamicResource TextSecondary}"/>
                    <Button Content="🔄 Actualizar" Style="{StaticResource ButtonStyle}" Margin="0,8,0,0"
                            Padding="10,5" Click="btnActualizar_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <DataGrid Grid.Row="2" x:Name="dgvUltimos"
                  Style="{StaticResource {x:Type DataGrid}}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  HeadersVisibility="Column" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha, StringFormat=dd/MM/yyyy HH:mm}" Width="140"/>
                <DataGridTextColumn Header="Concepto" Binding="{Binding Concepto}" Width="*"/>
                <DataGridTextColumn Header="Tipo" Binding="{Binding Tipo}" Width="90"/>
                <DataGridTextColumn Header="Monto" Binding="{Binding Monto, StringFormat=C2}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
