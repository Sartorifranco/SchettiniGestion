<Window x:Class="SchettiniGestion.WPF.CustomMessageBoxWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title=""
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        Foreground="{DynamicResource TextPrimary}"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        Topmost="True"
        SizeToContent="Height"
        MinWidth="300"
        MaxWidth="450"
        Loaded="Window_Loaded">

    <Window.Resources>
        <!-- Primario (Aceptar / Sí): acento tema -->
        <Style x:Key="ModernDlgPrimaryButton" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="MinWidth" Value="92"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <!-- Secundario suave (Cancelar / No cuando no es acción destructiva principal) -->
        <Style x:Key="ModernDlgOutlineButton" TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="MinWidth" Value="92"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource HoverBackground}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernDlgCloseChrome" TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="ToolTip" Value="Cerrar"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource HoverBackground}"/>
                    <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="{DynamicResource SurfaceDark}"
            CornerRadius="12"
            Padding="20"
            Margin="24"
            BorderBrush="{DynamicResource BorderColor}"
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="2" BlurRadius="16" Opacity="0.35" Direction="270" Color="#000000"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Título + cerrar -->
            <Grid Grid.Row="0" Margin="0,0,0,14">
                <TextBlock x:Name="lblTitulo"
                           Text="Mensaje"
                           FontSize="16"
                           FontWeight="Bold"
                           Foreground="{DynamicResource TextPrimary}"
                           TextWrapping="Wrap"
                           VerticalAlignment="Center"
                           Margin="0,2,36,0"/>

                <Button x:Name="btnCerrar"
                        Content="✕"
                        Style="{StaticResource ModernDlgCloseChrome}"
                        Click="btnCerrar_Click"/>
            </Grid>

            <!-- Cuerpo -->
            <ScrollViewer Grid.Row="1"
                          MaxHeight="320"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Margin="0,0,0,18">
                <TextBlock x:Name="lblMensaje"
                           Text=""
                           FontSize="14"
                           Foreground="{DynamicResource TextSecondary}"
                           TextWrapping="Wrap"
                           Margin="0,8,0,8"/>
            </ScrollViewer>

            <!-- Botones -->
            <StackPanel x:Name="panelBotones"
                        Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"/>
        </Grid>
    </Border>
</Window>
