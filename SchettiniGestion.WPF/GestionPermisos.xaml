<UserControl x:Class="SchettiniGestion.WPF.GestionPermisos"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SchettiniGestion.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="{DynamicResource ModuleBackgroundBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Gestión de Roles y Permisos" Grid.Row="0" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource HeaderForegroundBrush}" Margin="0,0,0,20"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{DynamicResource PanelBackgroundBrush}" CornerRadius="5" Padding="10" Margin="0,0,15,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Roles del Sistema" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource HeaderForegroundBrush}" Margin="0,0,0,10"/>

                    <ListBox x:Name="RolesListBox" Grid.Row="1"
                             Background="Transparent" 
                             BorderThickness="0" 
                             Foreground="{DynamicResource BodyForegroundBrush}"
                             FontSize="16"
                             SelectionChanged="RolesListBox_SelectionChanged">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                                <Setter Property="Padding" Value="10,5"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
                        <Button x:Name="btnNuevoRol" Content="+ Nuevo Rol" Click="btnNuevoRol_Click"
                                Background="#2563EB" Foreground="White" BorderThickness="0" Padding="10,5" Margin="0,0,10,0">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="4"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                        <Button x:Name="btnEliminarRol" Content="x Eliminar" Click="btnEliminarRol_Click"
                                Background="#DC2626" Foreground="White" BorderThickness="0" Padding="10,5">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="4"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Column="1" Background="{DynamicResource PanelBackgroundBrush}" CornerRadius="5" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Permisos Asignados" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource HeaderForegroundBrush}" Margin="0,0,0,10"/>

                    <TextBlock Grid.Row="1" x:Name="PermisosHelpText" 
                               Text="⬅️ Seleccione un rol de la izquierda para ver y editar sus permisos." 
                               FontSize="14" Foreground="{DynamicResource TextSecondary}" 
                               Margin="0,20,0,0" TextWrapping="Wrap"/>

                    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="PermisosStackPanel" Visibility="Collapsed" Margin="0,10,0,0">
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <Button x:Name="GuardarButton" 
                Content="Guardar Cambios de Permisos" 
                Grid.Row="2" 
                Style="{DynamicResource ActionButtonStyle}" 
                HorizontalAlignment="Right" 
                Margin="0,20,0,0"
                IsEnabled="False"
                Height="40" Padding="20,0" FontSize="16"
                Click="GuardarButton_Click"/>
    </Grid>
</UserControl>