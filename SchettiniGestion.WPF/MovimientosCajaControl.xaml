<UserControl x:Class="SchettiniGestion.WPF.MovimientosCajaControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BackgroundDark}"
             Loaded="Control_Loaded">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Movimientos de Caja" FontSize="22" FontWeight="Bold"
                       Foreground="{DynamicResource TextPrimary}"/>
            <TextBlock TextWrapping="Wrap" FontSize="12" Margin="0,6,0,0"
                       Foreground="{DynamicResource TextSecondary}"
                       Text="Consulta de ingresos, egresos y ventas del turno. Para cargar un ajuste manual de efectivo usá Caja → Ingresar / Retirar."/>
        </StackPanel>

        <Border Grid.Row="1" Background="{DynamicResource SurfaceDark}" CornerRadius="8"
                BorderBrush="{DynamicResource BorderColor}" BorderThickness="1"
                Padding="16,14" Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Desde" Style="{DynamicResource ModalCaptionTextStyle}" Margin="0,0,0,6"/>
                    <DatePicker x:Name="dpDesde" MinWidth="170" MinHeight="48"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Hasta" Style="{DynamicResource ModalCaptionTextStyle}" Margin="0,0,0,6"/>
                    <DatePicker x:Name="dpHasta" MinWidth="170" MinHeight="48"/>
                </StackPanel>
                <StackPanel Grid.Column="4">
                    <TextBlock Text="Tipo" Style="{DynamicResource ModalCaptionTextStyle}" Margin="0,0,0,6"/>
                    <ComboBox x:Name="cmbTipo"
                              Style="{StaticResource TouchComboBoxStyle}"
                              MinWidth="160"
                              MinHeight="48">
                        <ComboBoxItem Content="Todos" IsSelected="True"/>
                        <ComboBoxItem Content="Ingreso"/>
                        <ComboBoxItem Content="Egreso"/>
                    </ComboBox>
                </StackPanel>
                <StackPanel Grid.Column="6" VerticalAlignment="Bottom">
                    <TextBlock Text="Buscar concepto" Style="{DynamicResource ModalCaptionTextStyle}" Margin="0,0,0,6"/>
                    <TextBox x:Name="txtFiltro"
                             Style="{StaticResource ModuleTextBoxStyle}"
                             MinHeight="48"
                             TextChanged="txtFiltro_TextChanged"/>
                </StackPanel>
                <Button Grid.Column="8"
                        Content="🔍 Buscar"
                        Style="{StaticResource ButtonStyle}"
                        MinHeight="48"
                        MinWidth="120"
                        Padding="16,10"
                        VerticalAlignment="Bottom"
                        Click="btnBuscar_Click"/>
            </Grid>
        </Border>

        <DataGrid Grid.Row="2" x:Name="dgvMovimientos"
                  Style="{StaticResource ModuleDataGridStyle}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  HeadersVisibility="Column" CanUserAddRows="False" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha, StringFormat=dd/MM/yyyy HH:mm}" Width="140"/>
                <DataGridTextColumn Header="Concepto" Binding="{Binding Concepto}" Width="*"/>
                <DataGridTextColumn Header="Tipo" Binding="{Binding Tipo}" Width="90"/>
                <DataGridTextColumn Header="Monto" Binding="{Binding Monto, StringFormat=C2}" Width="120"/>
                <DataGridTextColumn Header="Usuario" Binding="{Binding Usuario}" Width="110"/>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="3" Background="{DynamicResource SurfaceDark}" CornerRadius="8"
                BorderBrush="{DynamicResource BorderColor}" BorderThickness="1"
                Padding="14,10" Margin="0,10,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="TOTAL INGRESOS" Foreground="{DynamicResource TextSecondary}" FontSize="11"/>
                    <TextBlock x:Name="lblIngresos" FontSize="16" FontWeight="Bold" Foreground="{StaticResource SuccessColor}"/>
                </StackPanel>
                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                    <TextBlock Text="TOTAL EGRESOS" Foreground="{DynamicResource TextSecondary}" FontSize="11"/>
                    <TextBlock x:Name="lblEgresos" FontSize="16" FontWeight="Bold" Foreground="{StaticResource DangerColor}"/>
                </StackPanel>
                <StackPanel Grid.Column="2" HorizontalAlignment="Right">
                    <TextBlock Text="SALDO NETO" Foreground="{DynamicResource TextSecondary}" FontSize="11"/>
                    <TextBlock x:Name="lblSaldo" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
