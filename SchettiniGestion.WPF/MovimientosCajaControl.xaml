<UserControl x:Class="SchettiniGestion.WPF.MovimientosCajaControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BackgroundDark}"
             Loaded="Control_Loaded">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Movimientos de Caja" FontSize="22" FontWeight="Bold"
                   Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,15"/>

        <Border Grid.Row="1" Background="{DynamicResource SurfaceDark}" CornerRadius="8" Padding="16,12" Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="14"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="14"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="14"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="14"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Desde" Style="{StaticResource LabelStyle}"/>
                    <DatePicker x:Name="dpDesde" Height="45" Width="140" FontSize="16"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Hasta" Style="{StaticResource LabelStyle}"/>
                    <DatePicker x:Name="dpHasta" Height="45" Width="140" FontSize="16"/>
                </StackPanel>
                <StackPanel Grid.Column="4">
                    <TextBlock Text="Tipo" Style="{StaticResource LabelStyle}"/>
                    <ComboBox x:Name="cmbTipo" Style="{StaticResource ModuleComboBoxStyle}" Width="130">
                        <ComboBoxItem Content="Todos" IsSelected="True"/>
                        <ComboBoxItem Content="Ingreso"/>
                        <ComboBoxItem Content="Egreso"/>
                    </ComboBox>
                </StackPanel>
                <TextBox Grid.Column="6" x:Name="txtFiltro" Style="{StaticResource ModernTextBox}"
                         VerticalAlignment="Bottom" TextChanged="txtFiltro_TextChanged"/>
                <Button Grid.Column="8" Content="🔍 Buscar" Style="{StaticResource ButtonStyle}"
                        VerticalAlignment="Bottom" Padding="12,8" Click="btnBuscar_Click"/>
            </Grid>
        </Border>

        <DataGrid Grid.Row="2" x:Name="dgvMovimientos"
                  Style="{StaticResource {x:Type DataGrid}}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  HeadersVisibility="Column" CanUserAddRows="False" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha, StringFormat=dd/MM/yyyy HH:mm}" Width="140"/>
                <DataGridTextColumn Header="Concepto" Binding="{Binding Concepto}" Width="*"/>
                <DataGridTextColumn Header="Tipo" Binding="{Binding Tipo}" Width="90"/>
                <DataGridTextColumn Header="Monto" Binding="{Binding Monto, StringFormat=C2}" Width="120"/>
                <DataGridTextColumn Header="Usuario" Binding="{Binding Usuario}" Width="110"/>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="3" Background="{DynamicResource SurfaceDark}" CornerRadius="8" Padding="14,10" Margin="0,10,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="TOTAL INGRESOS" Foreground="{DynamicResource TextSecondary}" FontSize="11"/>
                    <TextBlock x:Name="lblIngresos" FontSize="16" FontWeight="Bold" Foreground="{StaticResource SuccessColor}"/>
                </StackPanel>
                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                    <TextBlock Text="TOTAL EGRESOS" Foreground="{DynamicResource TextSecondary}" FontSize="11"/>
                    <TextBlock x:Name="lblEgresos" FontSize="16" FontWeight="Bold" Foreground="{StaticResource DangerColor}"/>
                </StackPanel>
                <StackPanel Grid.Column="2" HorizontalAlignment="Right">
                    <TextBlock Text="SALDO NETO" Foreground="{DynamicResource TextSecondary}" FontSize="11"/>
                    <TextBlock x:Name="lblSaldo" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
