<UserControl x:Class="SchettiniGestion.WPF.PlanillaDiariaControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BackgroundDark}"
             Loaded="Control_Loaded">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Planilla Diaria" FontSize="22" FontWeight="Bold"
                   Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,15"/>

        <Border Grid.Row="1" Background="{DynamicResource SurfaceDark}" CornerRadius="8"
                BorderBrush="{DynamicResource BorderColor}" BorderThickness="1"
                Padding="16,14" Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Fecha" Style="{DynamicResource ModalCaptionTextStyle}" Margin="0,0,0,6"/>
                    <DatePicker x:Name="dpFecha" MinWidth="190" MinHeight="48"/>
                </StackPanel>
                <Button Grid.Column="2"
                        Content="🔍 Ver planilla"
                        Style="{StaticResource ButtonStyle}"
                        MinHeight="48"
                        MinWidth="150"
                        Padding="16,10"
                        VerticalAlignment="Bottom"
                        Margin="0,0,10,0"
                        Click="btnVer_Click"/>
                <Button Grid.Column="3"
                        Content="📄 Exportar CSV"
                        Style="{StaticResource SecondaryButtonStyle}"
                        MinHeight="48"
                        MinWidth="150"
                        Padding="16,10"
                        VerticalAlignment="Bottom"
                        Click="btnExportar_Click"/>
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="{DynamicResource SurfaceDark}" CornerRadius="8"
                BorderBrush="{DynamicResource BorderColor}" BorderThickness="1"
                Padding="16,12" Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="VENTAS" Foreground="{DynamicResource TextSecondary}" FontSize="11"/>
                    <TextBlock x:Name="lblVentas" FontSize="18" FontWeight="Bold" Foreground="{StaticResource SuccessColor}"/>
                </StackPanel>
                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                    <TextBlock Text="COMPRAS" Foreground="{DynamicResource TextSecondary}" FontSize="11"/>
                    <TextBlock x:Name="lblCompras" FontSize="18" FontWeight="Bold" Foreground="{StaticResource DangerColor}"/>
                </StackPanel>
                <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                    <TextBlock Text="GASTOS" Foreground="{DynamicResource TextSecondary}" FontSize="11"/>
                    <TextBlock x:Name="lblGastos" FontSize="18" FontWeight="Bold" Foreground="{StaticResource DangerColor}"/>
                </StackPanel>
                <StackPanel Grid.Column="3" HorizontalAlignment="Center">
                    <TextBlock Text="INGRESOS CAJA" Foreground="{DynamicResource TextSecondary}" FontSize="11"/>
                    <TextBlock x:Name="lblIngresosCaja" FontSize="18" FontWeight="Bold" Foreground="{StaticResource SuccessColor}"/>
                </StackPanel>
                <StackPanel Grid.Column="4" HorizontalAlignment="Right">
                    <TextBlock Text="RESULTADO NETO" Foreground="{DynamicResource TextSecondary}" FontSize="11"/>
                    <TextBlock x:Name="lblNeto" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}"/>
                </StackPanel>
            </Grid>
        </Border>

        <DataGrid Grid.Row="3" x:Name="dgvMovimientos"
                  Style="{StaticResource ModuleDataGridStyle}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  HeadersVisibility="Column" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Hora" Binding="{Binding Fecha, StringFormat=HH:mm}" Width="70"/>
                <DataGridTextColumn Header="Concepto" Binding="{Binding Concepto}" Width="*"/>
                <DataGridTextColumn Header="Tipo" Binding="{Binding Tipo}" Width="90"/>
                <DataGridTextColumn Header="Monto" Binding="{Binding Monto, StringFormat=C2}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
