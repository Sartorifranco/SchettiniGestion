<UserControl x:Class="SchettiniGestion.WPF.ProductosControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100"
             Loaded="ProductosControl_Loaded">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
    <Grid Margin="20" MinWidth="560">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource SurfaceDark}" CornerRadius="8" Padding="15" Margin="0,0,0,10">
            <WrapPanel Orientation="Horizontal">
                <StackPanel MinWidth="220" MaxWidth="720" Width="420" Margin="0,0,16,8">
                    <TextBlock Text="Buscar por: Código, Código barras, Descripción, Rubro, SubRubro, Marca, Proveedor" Style="{StaticResource LabelStyle}" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtFiltro" Style="{StaticResource ModernTextBox}" TextChanged="txtFiltro_TextChanged"/>
                </StackPanel>
                <WrapPanel Orientation="Horizontal" VerticalAlignment="Bottom" Margin="0,0,0,8">
                    <Button x:Name="btnNuevo" Content="➕ Nuevo Producto" Style="{StaticResource ButtonStyle}" Padding="15,10" Margin="0,0,10,6" Click="btnNuevo_Click"/>
                    <Button x:Name="btnDescargarPlantilla" Content="📥 Plantilla CSV" Style="{StaticResource SecondaryButtonStyle}" Padding="10,8" Margin="0,0,10,6" Click="btnDescargarPlantilla_Click"/>
                    <Button x:Name="btnImportarExcel" Content="📗 Importar Masivo" Style="{StaticResource SecondaryButtonStyle}" Padding="10,8" Margin="0,0,0,6" Click="btnImportarExcel_Click"/>
                </WrapPanel>
            </WrapPanel>
        </Border>

        <Border Grid.Row="1" Background="{StaticResource SurfaceDark}" CornerRadius="8" Padding="1">
            <DataGrid x:Name="dgvProductos" Style="{StaticResource {x:Type DataGrid}}" 
                      AutoGenerateColumns="False" IsReadOnly="True"
                      SelectionChanged="dgvProductos_SelectionChanged"
                      MouseDoubleClick="dgvProductos_MouseDoubleClick">
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="✏️ Editar" Click="MenuItemEditar_Click"/>
                        <MenuItem Header="📋 Duplicar" Click="MenuItemDuplicar_Click"/>
                    </ContextMenu>
                </DataGrid.ContextMenu>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="COD" Binding="{Binding [Codigo]}" Width="Auto" MinWidth="70"/>
                    <!-- DataRowView: Path entre corchetes evita fallos de coincidencia con el descriptor -->
                    <DataGridTextColumn Header="DESCRIPCIÓN" Binding="{Binding [Descripcion]}" Width="*" MinWidth="120"/>
                    <DataGridTextColumn Header="RUBRO" Binding="{Binding [Categoria]}" Width="Auto" MinWidth="100"/>
                    <DataGridTextColumn Header="SUB-RUBRO" Binding="{Binding [SubRubro]}" Width="Auto" MinWidth="100"/>
                    <DataGridTextColumn Header="MARCA" Binding="{Binding [Marca]}" Width="Auto" MinWidth="80"/>
                    <DataGridTextColumn Header="PROVEEDOR" Binding="{Binding [Proveedor]}" Width="Auto" MinWidth="120"/>
                    <DataGridTextColumn Header="VENTA" Binding="{Binding [PrecioVenta], StringFormat=C2}" Width="Auto" FontWeight="Bold" Foreground="{StaticResource SuccessColor}"/>
                    <DataGridTextColumn Header="STK" Binding="{Binding [StockActual]}" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
    </ScrollViewer>
</UserControl>
