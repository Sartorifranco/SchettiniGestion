<UserControl x:Class="SchettiniGestion.WPF.ProveedoresControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1100"
             Loaded="ProveedoresControl_Loaded">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource SaaSTableCard}" Padding="18" Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Proveedores" Style="{DynamicResource TitleTextStyle}" Margin="0,0,0,10"/>
                    <TextBlock Text="Buscar por CUIT o Razón Social" Style="{DynamicResource LabelStyle}"/>
                    <TextBox x:Name="txtFiltroProveedores" Style="{DynamicResource ModernTextBox}" TextChanged="txtFiltroProveedores_TextChanged"/>
                </StackPanel>

                <Button Grid.Column="1" x:Name="btnNuevo" Content="➕ Nuevo Proveedor" Style="{StaticResource ButtonStyle}"
                        Padding="15,10" VerticalAlignment="Bottom" Margin="15,0,0,0" Click="btnNuevo_Click"/>
            </Grid>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource SaaSTableCard}" Padding="12,0,12,12">
            <DataGrid x:Name="dgvProveedores" Style="{DynamicResource {x:Type DataGrid}}" AutoGenerateColumns="False"
                      HorizontalScrollBarVisibility="Auto" IsReadOnly="True"
                      MouseDoubleClick="dgvProveedores_MouseDoubleClick">
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="✏️ Editar" Click="MenuItemEditar_Click"/>
                        <MenuItem Header="🗑️ Eliminar" Click="MenuItemEliminar_Click"/>
                    </ContextMenu>
                </DataGrid.ContextMenu>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="CUIT" Binding="{Binding CUIT}" Width="Auto" MinWidth="110"/>
                    <DataGridTextColumn Header="Razón social" Binding="{Binding RazonSocial}" Width="2*" MinWidth="160"/>
                    <DataGridTextColumn Header="Cat. fiscal" Binding="{Binding CategoriaFiscal}" Width="Auto" MinWidth="90"/>
                    <DataGridTextColumn Header="Contacto" Binding="{Binding PersonaContacto}" Width="Auto" MinWidth="110"/>
                    <DataGridTextColumn Header="Teléfono" Binding="{Binding Telefono}" Width="Auto" MinWidth="110"/>
                    <DataGridTextColumn Header="Domicilio" Binding="{Binding Direccion}" Width="1.5*" MinWidth="140"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="1.2*" MinWidth="140"/>
                    <DataGridTextColumn Header="Página web" Binding="{Binding PaginaWeb}" Width="1.2*" MinWidth="120"/>
                    <DataGridTextColumn Header="Saldo" Binding="{Binding SaldoDeuda, StringFormat=C2}" Width="Auto" MinWidth="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
