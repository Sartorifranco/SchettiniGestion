<UserControl x:Class="SchettiniGestion.WPF.ReportePresupuestosControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Background="{StaticResource BackgroundDark}"
             Loaded="UserControl_Loaded">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Historial de Presupuestos" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,20"/>

        <UniformGrid Grid.Row="1" Columns="5" Margin="0,0,0,20">
            <Button x:Name="btnHoy" Content="📅 HOY" Click="btnFiltroRapido_Click" Tag="Hoy" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,10,0" Height="45"/>
            <Button x:Name="btnAyer" Content="⬅ AYER" Click="btnFiltroRapido_Click" Tag="Ayer" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,10,0" Height="45"/>
            <Button x:Name="btnSemana" Content="📅 7 DÍAS" Click="btnFiltroRapido_Click" Tag="Semana" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,10,0" Height="45"/>
            <Button x:Name="btnMes" Content="📅 ESTE MES" Click="btnFiltroRapido_Click" Tag="Mes" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,10,0" Height="45"/>
            <Button x:Name="btnTodos" Content="♾️ HISTÓRICO" Click="btnFiltroRapido_Click" Tag="Todo" Style="{StaticResource SecondaryButtonStyle}" Height="45"/>
        </UniformGrid>

        <Border Grid.Row="2" Background="{StaticResource SurfaceDark}" CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="DESDE" Style="{StaticResource LabelStyle}"/>
                    <DatePicker x:Name="dtpDesde" MinWidth="180"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="HASTA" Style="{StaticResource LabelStyle}"/>
                    <DatePicker x:Name="dtpHasta" MinWidth="180"/>
                </StackPanel>

                <Button x:Name="btnBuscar" Grid.Column="4" Content="🔍 BUSCAR" Style="{StaticResource ButtonStyle}" MinWidth="140" VerticalAlignment="Bottom" Click="btnBuscar_Click"/>
            </Grid>
        </Border>

        <Border Grid.Row="3" Background="{StaticResource SurfaceDark}" CornerRadius="8" Padding="1">
            <DataGrid x:Name="dgvPresupuestos" Style="{StaticResource {x:Type DataGrid}}" AutoGenerateColumns="False" IsReadOnly="True" RowHeight="40">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="NRO" Binding="{Binding PresupuestoID, StringFormat=D8}" Width="100"/>
                    <DataGridTextColumn Header="FECHA" Binding="{Binding Fecha, StringFormat='dd/MM/yyyy HH:mm'}" Width="160"/>
                    <DataGridTextColumn Header="CLIENTE" Binding="{Binding RazonSocial}" Width="*"/>
                    <DataGridTextColumn Header="ESTADO" Binding="{Binding Estado}" Width="120"/>
                    <DataGridTextColumn Header="TOTAL" Binding="{Binding Total, StringFormat=C2}" Width="120" FontWeight="Bold" Foreground="{StaticResource SuccessColor}"/>

                    <DataGridTemplateColumn Width="190">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="👁️" ToolTip="Ver Detalle" Style="{StaticResource SecondaryButtonStyle}" Padding="0" Height="30" Width="40" Click="btnVer_Click" Margin="0,0,5,0"/>
                                    <Button Content="🖨️" ToolTip="Imprimir / PDF" Style="{StaticResource SecondaryButtonStyle}" Padding="0" Height="30" Width="40" Click="btnImprimir_Click" Margin="0,0,5,0"/>
                                    <Button Content="🛒" ToolTip="Pasar a venta" Style="{StaticResource SecondaryButtonStyle}" Padding="0" Height="30" Width="40" Click="btnPasarAVenta_Click" Margin="0,0,5,0"/>
                                    <Button Content="🗑️" ToolTip="Eliminar" Style="{StaticResource DeleteButtonStyle}" Padding="0" Height="30" Width="40" Click="btnEliminar_Click"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>