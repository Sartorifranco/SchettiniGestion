<UserControl x:Class="SchettiniGestion.WPF.ReportesControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Background="{DynamicResource BackgroundDark}"
             Loaded="ReportesControl_Loaded">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Reportes y Estadísticas" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,20"/>

        <UniformGrid Grid.Row="1" Columns="5" Margin="0,0,0,20">
            <Button x:Name="btnHoy" Content="📅 HOY" Click="btnFiltroRapido_Click" Tag="Hoy" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,10,0" Height="45"/>
            <Button x:Name="btnAyer" Content="⬅ AYER" Click="btnFiltroRapido_Click" Tag="Ayer" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,10,0" Height="45"/>
            <Button x:Name="btnSemana" Content="📅 7 DÍAS" Click="btnFiltroRapido_Click" Tag="Semana" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,10,0" Height="45"/>
            <Button x:Name="btnMes" Content="📅 ESTE MES" Click="btnFiltroRapido_Click" Tag="Mes" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,10,0" Height="45"/>
            <Button x:Name="btnAnio" Content="📅 ESTE AÑO" Click="btnFiltroRapido_Click" Tag="Anio" Style="{StaticResource SecondaryButtonStyle}" Height="45"/>
        </UniformGrid>

        <Border Grid.Row="2" Background="{DynamicResource SurfaceDark}" CornerRadius="8" Padding="20" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="DESDE" Style="{StaticResource LabelStyle}"/>
                    <DatePicker x:Name="dtpDesde" MinWidth="170" MinHeight="48"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="HASTA" Style="{StaticResource LabelStyle}"/>
                    <DatePicker x:Name="dtpHasta" MinWidth="170" MinHeight="48"/>
                </StackPanel>

                <Button x:Name="btnBuscar" Grid.Column="4" Content="🔍 GENERAR" Style="{StaticResource ButtonStyle}" Width="120" VerticalAlignment="Bottom" Height="40" Click="btnBuscar_Click"/>

                <Button x:Name="btnExportarExcel" Grid.Column="6" Click="btnExportarExcel_Click" 
                        Style="{StaticResource SecondaryButtonStyle}" 
                        Background="#1D6F42" BorderBrush="#14532D"
                        VerticalAlignment="Bottom" Height="40" Padding="20,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📗" Margin="0,0,10,0"/>
                        <TextBlock Text="EXPORTAR A EXCEL" FontWeight="Bold" Foreground="White"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <Border Grid.Row="3" Background="{DynamicResource SurfaceDark}" CornerRadius="8" Padding="1">
            <DataGrid x:Name="dgvReporte" Style="{StaticResource {x:Type DataGrid}}" AutoGenerateColumns="False" IsReadOnly="True" RowHeight="40">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="CÓDIGO" Binding="{Binding Codigo}" Width="Auto"/>
                    <DataGridTextColumn Header="PRODUCTO / SERVICIO" Binding="{Binding Descripcion}" Width="*"/>
                    <DataGridTextColumn Header="RUBRO" Binding="{Binding Rubro}" Width="Auto"/>
                    <DataGridTextColumn Header="UNIDADES" Binding="{Binding UnidadesVendidas}" Width="100"/>
                    <DataGridTextColumn Header="TOTAL VENDIDO" Binding="{Binding TotalVendido, StringFormat=C2}" Width="140" FontWeight="Bold" Foreground="{DynamicResource SuccessColor}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <TextBlock Text="TOTAL FACTURADO:" FontSize="18" Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBlock x:Name="lblTotalGeneral" Text="$ 0,00" FontSize="32" FontWeight="Bold" Foreground="{DynamicResource SuccessColor}"/>
        </StackPanel>
    </Grid>
</UserControl>