<UserControl x:Class="SchettiniGestion.WPF.UsuariosControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SchettiniGestion.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="#F0F0F0"
             Loaded="UsuariosControl_Loaded">


    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 1. CAMPOS DE ENTRADA Y BOTONES (Fila 0) -->
        <Border Grid.Row="0" Background="White" Padding="20" CornerRadius="5" BorderBrush="#CCC" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Fila 0: Usuario -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Nombre de Usuario:" Style="{StaticResource LabelStyle}"/>
                <TextBox x:Name="txtUsuario" Grid.Row="1" Grid.Column="0" Style="{StaticResource TextBoxStyle}"/>

                <!-- Fila 1: Contraseña -->
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Contraseña:" Style="{StaticResource LabelStyle}" Margin="0,10,0,0"/>
                <PasswordBox x:Name="txtPassword" Grid.Row="2" Grid.Column="1" Style="{StaticResource PasswordBoxStyle}" Margin="10,10,10,0"/>

                <!-- Fila 0: Rol -->
                <TextBlock Grid.Row="0" Grid.Column="1" Text="Rol:" Style="{StaticResource LabelStyle}"/>
                <ComboBox x:Name="cmbRol" Grid.Row="1" Grid.Column="1" Style="{StaticResource ComboBoxStyle}"/>

                <!-- Botones (en la columna 2) -->
                <StackPanel Grid.Row="0" Grid.Column="2" Grid.RowSpan="3" VerticalAlignment="Top" Margin="20,0,0,0">
                    <Button x:Name="btnNuevo" Content="Nuevo" Style="{StaticResource ButtonStyle}" Margin="0,5,0,0" Click="btnNuevo_Click"/>
                    <Button x:Name="btnGuardar" Content="Guardar" Style="{StaticResource ButtonStyle}" Margin="0,10,0,0" Click="btnGuardar_Click"/>
                    <Button x:Name="btnEliminar" Content="Eliminar" Style="{StaticResource ButtonStyle}" Margin="0,10,0,0" Click="btnEliminar_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 2. GRILLA DE USUARIOS (Fila 1) -->
        <DataGrid x:Name="dgvUsuarios" 
                  Grid.Row="1" 
                  Margin="0,15,0,0"
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionChanged="dgvUsuarios_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nombre de Usuario" Binding="{Binding NombreUsuario}" Width="*"/>
                <DataGridTextColumn Header="Rol" Binding="{Binding Rol}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>