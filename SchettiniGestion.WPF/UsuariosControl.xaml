<UserControl x:Class="SchettiniGestion.WPF.UsuariosControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Loaded="UsuariosControl_Loaded">

    <Grid Background="{DynamicResource BackgroundDark}">
        <TabControl Background="Transparent" BorderThickness="0" Margin="20">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border Name="Border" Padding="15,10" BorderThickness="0,0,0,2" BorderBrush="Transparent" Background="Transparent" Cursor="Hand">
                                    <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource PrimaryColor}"/>
                                        <Setter Property="Foreground" Value="{DynamicResource PrimaryColor}"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="False">
                                        <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>

            <TabItem Header="Usuarios">
                <Grid Margin="0,12,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3.5*" MinWidth="300" MaxWidth="450"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="6.5*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="{DynamicResource SurfaceDark}" CornerRadius="8" Padding="20">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="Gestión de Usuarios" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,20"/>

                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                <StackPanel>
                                    <TextBlock Text="NOMBRE DE USUARIO" Style="{StaticResource LabelStyle}"/>
                                    <TextBox x:Name="txtNombreUsuario" Style="{StaticResource ModernTextBox}" Margin="0,0,0,15"/>

                                    <TextBlock Text="CONTRASEÑA" Style="{StaticResource LabelStyle}"/>
                                    <PasswordBox x:Name="txtPassword" Style="{StaticResource PasswordBoxStyle}" Margin="0,0,0,15"/>

                                    <TextBlock Text="ROL" Style="{StaticResource LabelStyle}"/>
                                    <ComboBox x:Name="cmbRolesUsuario" Style="{StaticResource ModuleComboBoxStyle}"
                                              DisplayMemberPath="NombreRol" SelectedValuePath="RolID"
                                              Margin="0,0,0,15"/>
                                </StackPanel>
                            </ScrollViewer>

                            <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0,20,0,0">
                                <Button x:Name="btnNuevo" Content="✨ NUEVO" Style="{StaticResource SecondaryButtonStyle}" Click="btnNuevo_Click" Margin="0,0,0,10"/>
                                <Button x:Name="btnGuardarUsuario" Content="💾 GUARDAR USUARIO" Style="{StaticResource ButtonStyle}" Click="btnGuardarUsuario_Click" Margin="0,0,0,10" Height="45"/>
                                <Button x:Name="btnEliminar" Content="🗑️ ELIMINAR" Style="{StaticResource DeleteButtonStyle}" Click="btnEliminar_Click"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Border Grid.Column="2" Background="{DynamicResource SurfaceDark}" CornerRadius="8" Padding="1">
                        <DataGrid x:Name="dgvUsuarios" Style="{StaticResource {x:Type DataGrid}}" AutoGenerateColumns="False" IsReadOnly="True" SelectionChanged="dgvUsuarios_SelectionChanged">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding UsuarioID}" Width="60"/>
                                <DataGridTextColumn Header="USUARIO" Binding="{Binding NombreUsuario}" Width="*"/>
                                <DataGridTextColumn Header="ROL" Binding="{Binding NombreRol}" Width="Auto" MinWidth="100"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem Header="Permisos">
                <Grid Margin="15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="250"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <ListBox x:Name="lstRolesPermisos" Grid.Column="0" Margin="0,0,20,0"
                             Background="{DynamicResource SurfaceDark}"
                             Foreground="{DynamicResource TextPrimary}"
                             SelectionChanged="lstRolesPermisos_SelectionChanged"
                             DisplayMemberPath="NombreRol"/>

                    <StackPanel Grid.Column="1">
                        <ItemsControl x:Name="lstPermisosDinamicos">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Content="{Binding NombreModulo}"
                                              IsChecked="{Binding Habilitado, Mode=TwoWay}"
                                              Foreground="{DynamicResource TextPrimary}"
                                              Margin="0,8"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <Button x:Name="btnGuardarPermisos" Content="GUARDAR CAMBIOS DE PERMISOS"
                                Click="btnGuardarPermisos_Click"
                                Margin="0,20,0,0" Width="250" HorizontalAlignment="Left"
                                Background="#0078D7" Foreground="White" Padding="10"/>
                    </StackPanel>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>