<UserControl x:Class="SchettiniGestion.WPF.UsuariosControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Loaded="UsuariosControl_Loaded">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3.5*" MinWidth="300" MaxWidth="450"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="6.5*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource SurfaceDark}" CornerRadius="8" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Gestión de Usuarios" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,20"/>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="NOMBRE DE USUARIO" Style="{StaticResource LabelStyle}"/>
                        <TextBox x:Name="txtNombreUsuario" Style="{StaticResource ModernTextBox}" Margin="0,0,0,15"/>

                        <TextBlock Text="CONTRASEÑA" Style="{StaticResource LabelStyle}"/>
                        <PasswordBox x:Name="txtPassword" Style="{StaticResource PasswordBoxStyle}" Margin="0,0,0,15"/>

                        <TextBlock Text="ROL / PERMISOS" Style="{StaticResource LabelStyle}"/>
                        <ComboBox x:Name="cmbRoles" Style="{StaticResource ModuleComboBoxStyle}" 
                                  DisplayMemberPath="Nombre" SelectedValuePath="RolId" 
                                  Margin="0,0,0,15"/>
                    </StackPanel>
                </ScrollViewer>

                <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0,20,0,0">
                    <Button x:Name="btnNuevo" Content="✨ NUEVO" Style="{StaticResource SecondaryButtonStyle}" Click="btnNuevo_Click" Margin="0,0,0,10"/>
                    <Button x:Name="btnGuardar" Content="💾 GUARDAR" Style="{StaticResource ButtonStyle}" Click="btnGuardar_Click" Margin="0,0,0,10" Height="45"/>
                    <Button x:Name="btnEliminar" Content="🗑️ ELIMINAR" Style="{StaticResource DeleteButtonStyle}" Click="btnEliminar_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Column="2" Background="{StaticResource SurfaceDark}" CornerRadius="8" Padding="1">
            <DataGrid x:Name="dgvUsuarios" Style="{StaticResource {x:Type DataGrid}}" AutoGenerateColumns="False" IsReadOnly="True" SelectionChanged="dgvUsuarios_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding UsuarioID}" Width="60"/>
                    <DataGridTextColumn Header="USUARIO" Binding="{Binding NombreUsuario}" Width="*"/>
                    <DataGridTextColumn Header="ROL" Binding="{Binding NombreRol}" Width="Auto" MinWidth="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>